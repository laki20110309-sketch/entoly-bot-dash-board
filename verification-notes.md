# Verification notes

## Dashboard preview

ローカルの静的サーバーで `index.html` を表示し、概要画面のサイドバー、ヒーロー、4つの統計カード、最近のアクティビティ、クイックアクションが正常に表示されることを確認した。日本語テキスト、レスポンシブ用CSS、ボタン要素も取得できている。

## Reference specifications

- VCロール対象は固定名のボイスチャンネルで、`{teamname}`を含む動的なVC名は対象外。
- 条件なしのVCロールは複数付与可能で、最大5個。
- 条件付きルールは「条件ロールを持っていたら、付与ロールを付与」の組み合わせで、最大30件。
- 複数条件に一致した場合は、優先順位が最も高い1件だけ条件付きロールを付与する。
- VC参加中に所持ロールが変わった場合は再判定し、不要な条件付きロールを解除して一致するロールを付与する。
- 対象VCから退出すると、この機能で付与したロールを解除する。

参照URL:
- https://fn.yuyutti.com/ja/docs/register-now/voice-roles
- https://fn.yuyutti.com/ja/docs/register-now/role-grants

## Interaction checks

ロール管理画面では、固定VC名、条件なし付与ロール、条件付き付与ロール2件、最大30件の表示が確認できた。概要画面の「設定をリセット」操作では、実行前の二重確認モーダルが表示され、キャンセルとリセットの2つの操作が表示された。
